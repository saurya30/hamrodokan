//they cannot be created inside class

enum TextSizes {small, medium, large}

enum OrderStatus {processing, shipped, delivered}

enum PaymentMethods {esewa, khalti, creditCard, mobBank, masterCard, visaCard}
